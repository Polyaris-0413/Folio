package com.folio.read.ui.navigation

/**
 * 单 Activity 导航路由表(2.0.0)。
 * 命名统一 camelCase;阅读页是 main 目的地内的覆盖层,不在路由表里。
 */
object AppRoutes {
    const val MAIN = "main"
    const val LIBRARY_ADD = "libraryAdd"
    const val LICENSES = "licenses"
}
