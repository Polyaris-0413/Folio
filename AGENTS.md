# Folio 项目规范

## 代码约定

### 日志
- 一律通过 `com.folio.read.util.AppLog` 输出，禁止直接 import `android.util.Log`
- 级别语义：d=调试流水（release 构建自动剥离）、w=故障留痕、e=事故；tag 保持 Folio 前缀
- 失败后果延迟显现、用户当下无感知的关键失败路径至少记 w；设计内的正常回退分支（如 GBK 回退）不记日志，避免狼来了
- ColorOS 等部分 ROM 丢弃 d 级日志，需在那类设备上定位时临时改用 w（出处见 AppLog 注释）

## AGENTS.md 维护规则
- 仅允许追加两类内容：①AI 默认习惯会违反的项目约定；②踩坑结论（需跨会话生效的根因知识）
- 禁止写入任务状态、变更历史、计划进度——那些归 git log 与 docs/
- 规则必须一行一条、可执行，且与代码现状一致；现状已不成立的规则应立即修正或删除（过时规则会诱导 AI 去纠正存量代码）
- 宁缺毋滥：每条规则都会消耗每次会话的注意力，低价值规则会稀释高价值规则的执行

## 设计
项目所使用的设计语言为 Material Design；在设计界面时，需遵循 Material 3 技能的规范
路径为 C:\Users\Administrator.zcode\skills\material-3

## 更新日志

项目包含一个更新日志文件
路径为 C:\Users\Administrator\Desktop
名称为 Folio更新日志.txt

更新日志仅需记录对比上个已发布版本所进行的净更改 但在每次代码修改后都需判断是否记录日志

一个例子：
在 v1.0.0 的开发过程中
1. 加入了搜索功能
2. 修复了搜索功能带来的BUG
3. 微调了搜索功能

在 v1.0.0 的更新日志中 实际只需要在 changelog 中写下第一项即可 因为这些属于未公开功能的内部迭代

更新日志中有五个条目依次为：新增、改进、修复、调整、移除
请确保相关条目被放在了正确的位置

具体 changelog 格式可参考前文


